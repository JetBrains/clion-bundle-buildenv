diff --git a/bfd/configure b/bfd/configure
index ee588238500..49e0fc3a117 100755
--- a/bfd/configure
+++ b/bfd/configure
@@ -15403,7 +15403,14 @@ do
         all_targets=true
 	assocvecs="$assocvecs $targ_defvec $targ_selvecs"
     elif test $targ != plugin; then
-	. $srcdir/config.bfd
+	case "${host}" in
+	  aarch64-*-mingw*)
+	    # BFD config not available for aarch64 cross-builds
+	    ;;
+	  *)
+	    . $srcdir/config.bfd
+	    ;;
+	esac
 	if test $targ = $target; then
 	    defvec=$targ_defvec
 	fi
diff --git a/gdb/configure b/gdb/configure
index d0bdba6eb36..774d11a2d2f 100755
--- a/gdb/configure
+++ b/gdb/configure
@@ -24878,11 +24878,19 @@ CPPFLAGS=$OLD_CPPFLAGS
 # configuration.
 gdb_host_obs=posix-hdep.o
 
-if test "${target}" = "${host}"; then
-  gdb_native=yes
-else
-  gdb_native=no
-fi
+case "${host}" in
+  aarch64-*-mingw*)
+    # aarch64-mingw native support not yet available in configure.nat
+    gdb_native=no
+    ;;
+  *)
+    if test "${target}" = "${host}"; then
+      gdb_native=yes
+    else
+      gdb_native=no
+    fi
+    ;;
+esac
 
 . $srcdir/configure.host
 
@@ -25122,7 +25130,14 @@ gdb_osabi=
 targ=$target; . ${srcdir}/configure.tgt
 
 # Fetch the default architecture and default target vector from BFD.
-targ=$target; . $srcdir/../bfd/config.bfd
+case "${host}" in
+  aarch64-*-mingw*)
+    # BFD config not available for aarch64 cross-builds
+    ;;
+  *)
+    targ=$target; . $srcdir/../bfd/config.bfd
+    ;;
+esac
 
 # We only want the first architecture, so strip off the others if
 # there is more than one.
diff --git a/gdb/configure.host b/gdb/configure.host
index da71675..c39f327 100644
--- a/gdb/configure.host
+++ b/gdb/configure.host
@@ -107,6 +107,9 @@ i[34567]86-*-go32*)	gdb_host=go32 ;;
 i[34567]86-*-mingw32*)	gdb_host=mingw
 			gdb_host_obs=mingw-hdep.o
 			;;
+aarch64-*-mingw32*)	gdb_host=mingw
+			gdb_host_obs=mingw-hdep.o
+			;;
 i[34567]86-*-msdosdjgpp*) gdb_host=go32 ;;
 i[34567]86-*-linux*)	gdb_host=linux ;;
 i[34567]86-*-gnu*)	gdb_host=i386gnu ;;
diff --git a/gnulib/import/setenv.c b/gnulib/import/setenv.c
index ebfd4e5..b0e8cad 100644
--- a/gnulib/import/setenv.c
+++ b/gnulib/import/setenv.c
@@ -219,7 +219,11 @@ __add_to_environ (const char *name, const char *value, const char *combined,
 
       new_environ[size + 1] = NULL;
 
-      last_environ = __environ = new_environ;
+      memcpy ((char *) __environ, (char *) new_environ,
+        size * sizeof (char *));
+      __environ[size + 1] = NULL;
+
+      last_environ = new_environ;
     }
   else if (replace)
     {
@@ -315,7 +319,7 @@ clearenv (void)
     }
 
   /* Clear the environment pointer removes the whole environment.  */
-  __environ = NULL;
+  __environ[0] = NULL;
 
   UNLOCK;
 
diff --git a/libiberty/setenv.c b/libiberty/setenv.c
index d57e90c..99ee9c1 100644
--- a/libiberty/setenv.c
+++ b/libiberty/setenv.c
@@ -133,7 +133,11 @@ setenv (const char *name, const char *value, int replace)
 
       new_environ[size + 1] = NULL;
 
-      last_environ = __environ = new_environ;
+      memcpy ((char *) __environ, (char *) new_environ,
+        size * sizeof (char *));
+      __environ[size + 1] = NULL;
+
+      last_environ = new_environ;
     }
   else if (replace)
     {
diff --git a/opcodes/configure b/opcodes/configure
index 9dc87d6..a7e3fed 100755
--- a/opcodes/configure
+++ b/opcodes/configure
@@ -13928,7 +13928,14 @@ do
     if test "x$targ" = "xall" ; then
         all_targets=true
     else
-	. $srcdir/../bfd/config.bfd
+	case "${host}" in
+	  aarch64-*-mingw*)
+	    # BFD config not available for aarch64 cross-builds
+	    ;;
+	  *)
+	    . $srcdir/../bfd/config.bfd
+	    ;;
+	esac
 	selarchs="$selarchs $targ_archs"
     fi
 done
